# DocumentDB MCP Server Configuration

# Transport mode: stdio | sse | streamable-http
TRANSPORT=stdio

# Server configuration (only needed for HTTP transports)
HOST=localhost
PORT=8070

# DocumentDB/MongoDB connection string
DOCUMENTDB_URI=mongodb://localhost:27017