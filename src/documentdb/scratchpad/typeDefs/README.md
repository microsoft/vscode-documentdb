# Type Definitions — Moved

The `documentdb-shell-api.d.ts` file has been moved to the
`@vscode-documentdb/shell-api-types` package at:

    packages/shell-api-types/typeDefs/documentdb-shell-api.d.ts

The webpack build copies it to `dist/typeDefs/` from that package.
See `packages/shell-api-types/typeDefs/README.md` for documentation
on how the API surface was determined.
